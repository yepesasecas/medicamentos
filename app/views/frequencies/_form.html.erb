<%= form_for(@frequency) do |f| %>
  <% if @frequency.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@frequency.errors.count, "error") %> prohibited this frequency from being saved:</h2>

      <ul>
      <% @frequency.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :frecuencia %><br>
    <%= f.text_field :frecuencia %>
  </div>
  <div class="field">
    <%= f.label :horas_frecuencia %><br>
    <%= f.number_field :horas_frecuencia %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
